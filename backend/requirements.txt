# Core dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0

# Database
motor>=3.3.0
beanie>=1.25.0

# Vector DB & RAG
chromadb>=0.4.22
fastembed>=0.2.0

# Agent framework — pinned to avoid pip backtracking
crewai>=1.9.3,<2.0
openai>=1.83.0,<1.84
instructor>=1.7.0,<1.8.0
jiter>=0.8.0,<0.9

# LLM providers
google-genai

# Auth
firebase-admin>=6.4.0

# Rate limiting
slowapi>=0.1.9
# redis is optional — rate_limit.py falls back to in-memory when unavailable
redis>=5.0.0

# Observability — OPTIONAL: install manually with `pip install opik`
# Pulls in litellm (~large). All imports are wrapped in try/except.
# opik>=1.0.0,<2.0

# Utils
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0
httpx>=0.26.0

# Dev/Test — not included in Docker prod image
# pytest-asyncio>=0.21.0
